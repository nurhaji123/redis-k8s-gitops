api:
  replicaCount: 3

  image:
    repository: ghcr.io/nurhaji123/redis-assignment-api
    tag: "9063817f1d7ebd44270a3265600a8c1bf080182a"
    pullPolicy: IfNotPresent

  containerPort: 8000

  service:
    type: ClusterIP
    port: 80
    targetPort: 8000

redis:
  replicaCount: 1

  image:
    repository: redis
    tag: "7"
    pullPolicy: IfNotPresent

  containerPort: 6379

  service:
    type: ClusterIP
    port: 6379
    targetPort: 6379

config:
  redisHost: redis
  redisPort: "6379"

secret:
  redisPassword: example-password

ingress:
  enabled: true
  className: nginx
  host: localhost
  path: /
  pathType: Prefix
